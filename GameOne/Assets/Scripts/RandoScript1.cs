using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RandoScript1 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //This is my random script
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
